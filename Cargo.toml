[package]
name = "ciris-status"
version = "0.3.59"
edition = "2021"
description = "The ciris.ai public health/status surface — now a ciris-server fabric node + a StatusAdapter (mirrors CIRISAgent's adapter model). Serves /health, /v1/status, /api/v1/status, /api/v1/status/history + the public scoring roster (Flow A, own corpus) + live SSE/WS, by live outbound probes + a SQLite uptime history. The federation node (engine, edge, consent:replication, read API, ownership, safety, NAT-traversal) is ciris-server's serve; the status page is the adapter."
license = "AGPL-3.0-or-later"
publish = false

[[bin]]
name = "ciris-status"
path = "src/main.rs"

[dependencies]
# ── The fabric node ──────────────────────────────────────────────────────────
# ciris-status IS a ciris-server node now (no optional `fabric` feature): the
# whole node — engine + edge + consent:replication + read API + NodeCode +
# ownership + safety + NAT-traversal — is ciris-server's `serve_with_adapter`,
# and the status page is a `StatusAdapter: ciris_server::Adapter` folded onto the
# SAME shared core. Default features (NOT python, NOT pkcs11).
# Pinned to ciris-server v0.5.189 — THE MESH RELIABILITY CUT, and the one this
# crate has a direct operational stake in twice over.
#
# 1. THE DUPLICATE STORM IS FIXED (CIRISPersist#771). This node's own canonical
#    peer refused 7,536 attestation envelopes in a six-hour window — 428 distinct
#    rows re-sent up to 82 times each — because `put_attestation` reported an
#    already-held row as `federation_backend`, a backend ERROR, so the sender
#    never learned it had landed and retried forever. 58% of all refusals on that
#    node, and pure churn on the transport ciris-status watches. persist v38.6.0
#    returns `AlreadyHeld`; edge routes it to `ApplyOutcome::Duplicate` — counted,
#    not swallowed.
#
# 2. THE ACTOR/NODE KEY SPLIT (CC 3.4.7.3, CIRISConstitution#95). A node's
#    substrate identity was whatever key the host handed it, and in the embedded
#    fold that is an `identity_type = agent` key. So the Reticulum transport
#    identity, `self_key_id` and the de-admission self all named a key carrying
#    AGENCY — and persist's agency gate constrains only a NODE-ONLY recipient, so
#    the fusion did not blur "infrastructure must not have agency", it switched
#    the rule off.
#
#    A node now mints its own `<alias>-node` key, registers it `identity_type =
#    node`, takes the owner-binding, and authors its replication consent with its
#    own signer. Provisioning is a READINESS GATE: edge must not start on a half
#    identity. ciris-status is a fabric node, so this is its identity too.
#
# The client is a PACKAGE, not a tree (CIRISServer#471): `client/` is deleted —
# 1,813 files, 764k lines — and `ciris-client==0.5.189` comes from PyPI. Server
# and client versions now move together, gated: a node shipping an older client
# greets its own operator with "Update recommended" against the dependency it was
# built with.
#
# 0.5.196 IS THE REASON FOR THIS CUT, and unlike 0.5.194 the headline IS
# inherited — checked rather than assumed, because .194's whole lesson was that
# embedding inherits what lives inside the server and nothing the binary builds
# for itself. `serve` delegates to `serve_with_adapter`, and the new background
# scheduler is constructed inside that body, so ciris-status gets it by calling
# the function it already calls.
#
# THE SUBSTRATE MOVES: persist v38.7.0 / edge v18.12.0 / verify v14.0.0. Verify is
# a one-major jump and it arrives here TRANSITIVELY — this repo pins only
# ciris-server, so there is no second copy to keep in step (in CIRISServer itself
# the same bump touched 13 pins across two Cargo.toml files and produced two
# copies of one crate until the workspace member was found).
#
# edge v18.12.0 is what matters most on this host. CIRISEdge#547: the advertise
# path's holdings scan now reads the INDEX — ~1.6 MB per round instead of ~1.3 GB
# — which is the measured cause of CIRISServer#506, the read API going deaf after
# ~22h with one thread parked in `D` on `wait_on_page_bit_common`, all threads at
# 0% CPU, and 81-118 MB/s of sustained disk read. The worker floor could not help
# there: more workers add queue positions, not throughput. ciris-status shares the
# canonical with six other containers, so it was paying that I/O too.
#
# THE LOAD CEILING NOW EXISTS, and this node is a plausible first subject.
# persist v38.7.0 registers `mesh_config` key `load.ceiling`, and CIRISServer
# wires it to a real bound: retention passes take a slot in a background
# scheduler, floored at 1 because relief reaching zero is a halt rather than
# relief, and shrinking DRAINS rather than cancelling in-flight work. It is CC
# 4.2.1's graded "do less work" tier — a trust root can throttle this node
# without halting it, TTL-bounded so it recovers on its own. On a shared 2-vCPU
# box that is the difference between degrading and being restarted.
#
# THE IDENTITY SURFACE STOPS LYING. 0.5.195 named `key_id` the NODE key while the
# signing and content-KEM pubkeys stayed the ENGINE's, so a consumer verifying a
# fingerprint used the wrong key. .196 keeps `key_id` as the node's address and
# adds `key_material_key_id` naming whose material the pubkeys are. ciris-status
# is node-only, so the two agree here — but they now SAY they agree instead of
# being silently assumed to.
#
# NO MIGRATION for this node's key. .195 made `ciris-node-bootstrap` the base
# alias for every node, and .196 probes for a legacy `<host>-node` sealed blob and
# ADOPTS it when present. An existing ciris-status node keeps its key, its
# destination hash and its peer routes; only a node with no node key yet takes the
# new base alias.
#
# 0.5.194 was the previous cut, and its floor is still not inherited —
# it had to be adopted in this repo's own `main`. A default tokio runtime sizes to
# core count, so on the 2-vCPU canonical this binary got TWO workers: a two-slot
# budget for the accept loop, replication, the scorer and every request at once.
# One blocking task and HTTP is unschedulable — the socket stays LISTEN while
# `Recv-Q` climbs and userspace never calls `accept()`. Measured on that host with
# one worker pegged at 99.9% and five threads idle (CIRISServer#501, which
# reproduced on 0.5.192 and was NOT a .193 regression).
#
# ciris-status calls `serve_with_adapter`, so it inherits every fix INSIDE the
# server and none of the floor, which lives in the runtime the binary builds for
# itself. `#[tokio::main]` is therefore replaced with
# `ciris_server::node_runtime::build` — floor of 4, never a cap, and a deliberate
# `TOKIO_WORKER_THREADS` still honoured. It shares the canonical with six other
# containers, so this is the difference between degrading and disappearing.
#
# The other half of #501 IS inherited: boot prime was awaited inline while
# `read_api_bind` sat ~180 lines below, so time-to-serving scaled with the
# directory — 11,034 transport_destinations against 748 keys on the canonical, 33s
# of boot with the socket bound and nothing accepted. Now spawned and yielding per
# batch. Plus `announced_peers` bounded at 30 days; `transport_destinations`
# deliberately NOT pruned, because all 11,034 rows are assertions rather than
# observations and pruning would strand a node on a canonical route it cannot
# relearn.
#
# 0.5.193 — the unregistered actor key stops being fatal. A node carrying an agent
# whose actor key was absent from `federation_keys` had every row it authored
# refused at every peer (1,943 refusals in one 30-minute window). .192 diagnosed
# that and BAILED, which only moved the failure somewhere readable; .193 registers
# instead, needing no new key material since the actor key IS the engine's own
# identity. ciris-status carries no agent, so it takes the fix as insurance rather
# than as a cure — but it also takes the OCCURRENCE model: occurrences are
# independent by default, and nothing here is joined to anything.
#
# 0.5.192 — the client version DECOUPLED: `ciris-client>=0.5.190,<0.6` replaces
# `==<this cut>`, so the client can ship for the agent team without a paired server
# cut. The range is tested at BOTH ends, and 0.5.190 is the floor because the WHEEL
# MATRIX sets it, not the API. 0.5.191 paired with client 0.5.191 and closed the
# gate that let #493 ship at all.
#
# Substrate underneath is UNCHANGED across all four: edge v18.11.0 + persist
# v38.6.0 + verify v13.6.1. Every one of these cuts is server-side only.
#
# Prior: v0.5.189 = THE NODE/ACTOR KEY SPLIT — a node mints its own `<alias>-node`
# key, registers it `identity_type = node`, takes the owner-binding, and authors
# its replication consent with its own signer. Provisioning is a READINESS GATE:
# edge must not start on a half identity. ciris-status is a fabric node, so this is
# its identity too.
#
# 0.5.197 IS THE REASON FOR THIS CUT: the fabric stops authoring other people's
# claims. persist v40.0.0 / edge v20.1.1 / verify v14.1.0, all TRANSITIVE through
# this one pin. `attestation_promote` is GONE upstream — the two motions are
# `enter_mesh` (tier crossing over the same bytes, node co-scrub only) and
# `widen_audience` (a `supersedes` the ACTOR signs at the wider audience) — and
# ciris-status inherits the composed crossing the same way it inherits `serve`.
# Two things this node gets by being a node: the ANNOUNCE now logs and returns the
# identity bundle federation discovery walks (3 rows for a fabric node like this
# one) with `federation_discoverable`, and a node that has not announced is named
# as P2P-only in its own logs instead of surfacing three stages later as a
# withheld row. Both mesh ladders (traceflow + cross-node chat) are green in
# CIRISServer's CI on two sovereign nodes for the first time. The client floor
# moved to ciris-client 0.5.202 (the first bundle that defines the chat handshake
# ids); this node greets its operator against that floor.
ciris-server = { git = "https://github.com/CIRISAI/CIRISServer", tag = "v0.5.197" }

# NO ciris-persist PIN. persist arrives through ciris-server's `pub use
# ciris_persist` re-export, so this crate cannot resolve a different persist
# than the server it embeds. A second git pin here would be a second source of
# truth for one substrate: cargo resolves both, and `Engine` from one is then a
# different TYPE from `Engine` in the other — the CIRISPersist#109 cross-instance
# shape, arriving by the source-tree door. Reach persist as
# `ciris_server::ciris_persist::…` (each module aliases it with one `use`).

# Impl `ciris_server::Adapter` (async fn in trait). async-trait 0.1 + axum 0.8
# match ciris-server.
async-trait = "0.1"

# emit_liveness signs + assembles the federation `Attestation` row: base64 for
# the signature wire form, sha2 for the original_content_hash anchor, hex for the
# digest encoding. All already in the lock as substrate-transitive deps.
base64 = "0.22"
sha2 = "0.10"
hex = "0.4"

axum = { version = "0.8", features = ["ws"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "signal", "sync"] }
tokio-stream = { version = "0.1", features = ["sync"] }
# rustls (not openssl) so the binary is self-contained and cross-builds cleanly.
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
# Pinned to 0.31 (libsqlite3-sys 0.28) to match ciris-persist v9.10.0 (both declare `links = "sqlite3"`,
# so cargo requires a single libsqlite3-sys across the graph).
rusqlite = { version = "0.31", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde", "clock"] }
tower-http = { version = "0.6", features = ["cors"] }
futures = "0.3"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1"

[profile.release]
strip = "symbols"

